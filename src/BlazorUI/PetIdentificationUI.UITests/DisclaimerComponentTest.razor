@inherits TestComponentBase

<Fixture Test="Does_DisclaimerComponent_Render_Correctly">
    <ComponentUnderTest>
        <Disclaimer />
    </ComponentUnderTest>

@code
{
    void Does_DisclaimerComponent_Render_Correctly(Fixture fixture)
    {
        //Act
        var context = fixture.GetComponentUnderTest<Disclaimer>();

        context.MarkupMatches(InstanceFactory.DisclaimerExpectedHtml);
    }
}
</Fixture>