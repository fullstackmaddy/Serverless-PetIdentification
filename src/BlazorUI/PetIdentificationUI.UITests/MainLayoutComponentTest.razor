@inherits TestComponentBase

<Fixture Test="Does_MainLayoutComponent_Render_Correctly">
    <ComponentUnderTest>
        <MainLayout />
    </ComponentUnderTest>

@code
{
    void Does_MainLayoutComponent_Render_Correctly(Fixture fixture)
    {
        //Act
        var context = fixture.GetComponentUnderTest<MainLayout>();

        context.MarkupMatches(InstanceFactory.MainLayoutExpectedHtml);
    }
}
</Fixture>