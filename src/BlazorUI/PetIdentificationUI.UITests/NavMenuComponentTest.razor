@inherits TestComponentBase

<Fixture Test="Does_NavMenuComponent_Render_Correctly">
    <ComponentUnderTest>
        <PetIdentificationUI.Shared.NavMenu />
    </ComponentUnderTest>

@code
{
    void Does_NavMenuComponent_Render_Correctly(Fixture fixture)
    {
        //Act
        var context = fixture.GetComponentUnderTest<PetIdentificationUI.Shared.NavMenu>();

        context.MarkupMatches(InstanceFactory.NavMenuExpectedHtml);
    }
}
</Fixture>